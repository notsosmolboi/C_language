#include<stdio.h>
void swap(int *ptr1,int *ptr2)
{
    
    int *temp;
    printf("before swapping a = %d b = %d \n",*ptr1,*ptr2);
    temp=ptr2;
    ptr2=ptr1;
    ptr1=temp;
    printf("after swapping a = %d b = %d ",*ptr1,*ptr2);
}  
int main()
{
   int a=10,b=40;
   swap(&a,&b);
   return 0;
}
