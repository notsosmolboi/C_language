#include <stdio.h>
int facr(int n);
int fact(int n){
    int res;
    if(n==0){
        res=1;
        return res;
    }
    return n*fact(n-1);
}
int main() {
   int n;
   scanf("%d",&n);
   printf("%d",fact(n));
    return 0;
}
