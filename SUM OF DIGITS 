#include<stdio.h>
#include<conio.h>
void main(){
    int a,sum=0;
    printf("Enter a number:");
    scanf("%d",&a);
    while(1){
        sum+=a%10;
        a/=10
        if(a<1)
            break;
    }
    printf("Sum is %d",sum);
}